# nixpacks.toml
[phases.setup]
nixPkgs = ["python39", "nodejs_20"] # Define as versões que usas

[phases.install]
cmds = ["npm install", "pip install -r requirements.txt"]

[start]
cmd = "python main.py"